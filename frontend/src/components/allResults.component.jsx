import "./allResults.styles.scss";

const AllResults = ({ results }) => {
  console.log("data from all results: ---->", results);
  return (
    <div>
      <h1>Data did pass through</h1>
    </div>
  );
};

export default AllResults;
